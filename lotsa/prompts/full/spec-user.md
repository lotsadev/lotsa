## Task: {title}

{body}

## Pre-seeded draft spec (if any)

A draft spec from a prior conversation may appear below. If it reads
"(not available)", there is no draft — elicit the spec from scratch. Otherwise,
verify and finalize it per the "Spec already present?" instructions.

{artifact:draft_spec}
