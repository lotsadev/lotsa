## Task: {title}

{body}

## Carried spec (if any)

A spec distilled during chat may be carried here. It is context, not a
contract that overrides the task body above — use it when present, and ignore
the "(not available)" placeholder when it isn't.

{artifact:draft_spec}
